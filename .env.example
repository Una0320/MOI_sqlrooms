# 後端資料伺服器位址（需支援 Range Requests）
# 實際檔名依畫面上的情境切換按鈕動態決定，對照表在 src/constants/data.ts 的 SCENARIO_CONFIG。
VITE_DUCKDB_CONNECTION_STRING=http://localhost:7780

# 選填：MapTiler API key（免費申請：https://cloud.maptiler.com/account/keys/）。
# 底圖用 MapTiler 的 dataviz-dark 樣式，OSM 資料每週重建一次；不填就 fallback 回 CARTO
# dark-matter（資料官方文件寫最慢一年才更新一次，新完工的路橋可能好幾個月看不到）。
VITE_MAPTILER_API_KEY=
